version: "3.8"

services:
  seeclearly:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - ./models:/app/models  # Mount models directory for easy model updates
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
